---
const { code, title, subtitle, more } = Astro.props;
---
<div class="section-head" data-reveal>
  <div class="heading">
    <span class="eng">{code}</span>
    <h2>{title}</h2>
    {subtitle && <p>{subtitle}</p>}
  </div>
  {more && <a class="more" href={more.href}>{more.label}</a>}
</div>
